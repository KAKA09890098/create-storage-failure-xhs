---
name: create-storage-failure-xhs
description: 批量创作原创、生活化、可执行的存储设备故障小红书科普内容，并生成AI分步配图、图片卡片、独立Word、合集Word、检查PDF、故事板和完整压缩包。用于用户提到硬盘、SSD、移动硬盘、U盘、存储卡、Windows或macOS存储故障，要求数据恢复科普、小红书图文、批量文章、分步排查、Word/PDF排版、配图或整套交付时。始终把保护数据放在修复设备之前。
---

# 存储故障小红书图文批量创作

## 总目标

根据用户指定的设备、故障场景、文章数量和输出格式，生产原创、生活化、可执行的数据故障科普内容。优先保护数据，避免诱导普通用户执行可能造成二次破坏的操作。

## 必读规范

开始创作前读取：

- [安全与技术规范](references/safety-and-technical-rules.md)：判断停电、停写、禁止操作和专业词类比。
- [文章与交付规范](references/content-and-delivery-spec.md)：选题矩阵、固定文章结构、配图、Word、PDF和质检要求。

两份规范均为强制要求。若用户明确要求与默认值冲突，以用户要求为准；但不得降低数据安全底线、伪造来源或跳过用户明确要求的文件验证。

## 执行流程

### 1. 确认任务参数

提取或合理推断文章数量、设备范围、操作系统、目标读者、每篇图片数、总结长度、Word形式及是否需要小红书卡片。

信息不足且不会实质改变方案时使用默认值：面向无维修经验的小白，同时覆盖Windows和macOS，每篇5张图，每篇总结480—600字，同时生成独立Word和合集Word。只有缺失信息会明显改变工作量或交付形式时才询问用户。批量任务较大时可分批执行，但不得擅自减少总量。

### 2. 联网核验技术事实

核对会影响安全或操作结果的事实，优先采用Microsoft、Apple、Seagate、Western Digital、Samsung、SanDisk、SD Association及对应设备厂商的官方支持资料。

社交平台只用于分析用户常搜故障、误操作、标题结构和内容节奏。不得复制原文、标题或固定表达。每篇仅列实际查阅且直接支持正文的资料，不得虚构来源。

### 3. 建立选题矩阵

先生成去重选题表，再撰写文章。分散覆盖识别、显示、装载、文件系统、读写、物理损伤、误操作和跨系统兼容问题。不得只替换设备名称、错误代码或少量词语。

### 4. 撰写并自检文章

严格使用参考规范中的13段固定结构。专业词第一次出现时立即给生活类比，再解释真实技术含义。Windows和macOS步骤必须做到一个步骤只执行一个动作，并说明“点哪里、看什么、结果代表什么、何时停手”。

先完成正文事实检查与安全检查，再生成配图，避免图片和文字建立在错误操作上。

### 5. 生成配图与卡片

每篇默认生成5个不同场景：故障现场、生活比喻、Windows排查、Mac或交叉验证、急救止损。使用统一视觉语言，不生成品牌Logo，不依赖AI生成可读操作文字；准确中文标题在后期排版时叠加。

如环境存在专门的小红书卡片工具，优先用于分页与渲染；不存在时使用现有图像、文档或排版能力制作等规格卡片，不得省略交付。按批次生成时，核对每张图与对应文章、页码和场景一致。

### 6. 生成并验证文件

按每篇固定6页生成独立Word，并生成全部文章合集Word、AI故事板、分步场景图和完整压缩包。使用文档能力创建DOCX，使用PDF能力导出、渲染和逐页检查。

不要把“已生成文件”当作“已验证文件”。打开或解析每个独立Word；检查合集文章数、页数、图片数和表格数；实际测试压缩包完整性。发现问题后修正、重新导出并再次检查。

### 7. 交付

简要报告文章数量、配图数量、Word数量、合集页数、总结字数范围、是否完成逐页检查。至少提供合集Word与完整压缩包的可点击链接；用户要求时同时提供独立Word、PDF、故事板或图片目录。

## 不可降低的标准

- 保护数据优先于修好设备。
- 不把初始化、格式化、新建卷、CHKDSK或First Aid描述成无风险操作。
- 不以“试一下”为理由反复通电、反复插拔或继续写入异常设备。
- 不伪造官方来源、检查结果、文件数量或视觉验证。
- 不输出需要用户再次删除提示语、占位符或制作说明才能发布的成品。
- 无法使用某个可选工具时采用可用能力降级完成；核心交付确实无法完成时，明确说明缺失项，不假装完成。
