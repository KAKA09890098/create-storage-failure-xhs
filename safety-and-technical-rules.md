# 安全与技术规范

## 目录

1. 核心安全原则
2. 必须停止通电或写入的情况
3. 高风险操作表述
4. 专业词生活类比
5. 小白步骤写法
6. Windows最低覆盖范围
7. macOS最低覆盖范围

## 1. 核心安全原则

始终把“保护数据”放在“修好设备”之前。先判断设备是否适合继续通电、读取或写入，再安排系统排查。设备中有重要资料时，以只读观察、减少通电次数和避免写入为基本原则。

## 2. 必须停止通电或写入的情况

遇到以下情况，明确要求用户停止通电或停止写入，并说明继续尝试可能扩大物理损伤或覆盖原数据：

- 机械硬盘咔哒、摩擦、蜂鸣或无法正常起转。
- 设备进水、摔落、出现焦味或异常发烫。
- 容量乱跳、持续掉线或读取速度不断下降。
- 闪存设备显示0字节、无媒体或永久只读。
- 有资料的磁盘显示RAW、未分配或未初始化。

物理异常设备不要指导普通用户拆盘、开盘、敲击、冷冻、反复上电或自行更换内部部件。

## 3. 高风险操作表述

不得把以下操作描述成无风险修复：初始化磁盘、格式化、新建卷、重新分区、Windows CHKDSK、macOS First Aid（急救）、写入式坏道修复、量产、固件刷新或安全擦除。

必须说明这些操作可能改写分区、文件系统、目录或数据区域。若资料重要，应先停止写入，优先制作只读镜像或交由专业人员评估。不得为了让设备“重新显示”而牺牲恢复可能性。

## 4. 专业词生活类比

专业词第一次出现时，立即使用下列映射解释，然后补充真实含义，避免把比喻当作技术事实：

| 专业词 | 生活类比 | 真实含义提示 |
| --- | --- | --- |
| 存储设备 | 仓库 | 保存文件的硬件介质 |
| 数据 | 仓库里的货物 | 文件内容及其结构信息 |
| USB线 | 通往仓库的公路 | 传输数据并可能承担供电的连接线 |
| 接口 | 仓库门口 | 设备与主机连接的物理或逻辑入口 |
| 供电 | 开门和启动机器的电力 | 设备正常启动所需电压与电流 |
| 盘符或装载点 | 仓库门牌 | 操作系统访问卷的入口名称或路径 |
| 分区 | 仓库内部划分的房间 | 磁盘空间的逻辑区域 |
| 文件系统 | 货物登记账本 | 组织、定位和管理文件的数据结构 |
| 目录 | 仓库货物清单 | 文件与文件夹的层级和索引信息 |
| 扇区或存储单元 | 单个货位 | 介质保存数据的基础位置 |
| 坏扇区 | 塌掉或无法使用的货架 | 无法稳定读取或写入的存储区域 |
| 删除 | 从账本上划掉货物记录 | 通常先移除引用，原内容未必立即清零 |
| 格式化 | 重做仓库账本和货位编号 | 重建文件系统结构，可能破坏原索引并伴随覆盖 |
| 覆盖写入 | 新货压在旧货位置上 | 新数据占用旧数据所在区域，降低恢复可能 |
| 只读 | 只允许出库、不允许入库 | 系统或设备拒绝写入修改 |
| 镜像 | 按原样复制整座仓库 | 按扇区复制介质内容到另一介质或镜像文件 |
| 数据恢复 | 根据残留货物和旧账本重新找货 | 从残存内容和元数据重建或提取文件 |

## 5. 小白步骤写法

每个编号步骤只做一个动作，并同时说明：点哪里、看哪个字段或状态、不同结果代表什么、哪种结果必须立即停手。

不得只写“检查磁盘状态”“修复文件系统”“换接口试试”等模糊指令。交叉验证时控制次数；出现异响、发热、掉线加重、速度持续下降或容量异常，立即停止。

## 6. Windows最低覆盖范围

至少覆盖并给出明确路径：

- 设备管理器：设备是否枚举、是否有警告标记。
- 磁盘管理：磁盘是否出现、容量是否正确、是否显示RAW、未分配、未初始化或脱机。
- 盘符状态：卷是否缺少盘符。
- 容量和文件系统状态：容量是否合理、文件系统是否可识别、是否提示格式化。

看到RAW、未分配、未初始化、0字节、容量明显错误或频繁掉盘时，停止写入，不执行初始化、格式化、新建卷或CHKDSK。

## 7. macOS最低覆盖范围

至少覆盖并给出明确路径：

- Finder设置：外置磁盘是否允许显示在桌面或侧边栏。
- 系统信息：USB、Thunderbolt或对应总线下是否识别硬件。
- 磁盘工具：设备或卷是否出现。
- “显示所有设备”：区分物理设备、容器和卷。
- 装载状态：卷是否已装载、是否能读取。

First Aid可能修改文件系统，不得把它当作资料重要时的第一步。看到容量异常、设备反复消失、无法稳定读取或明显物理异常时，停止进一步尝试。

